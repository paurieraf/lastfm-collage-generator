# setup-library-debug-environment

Set up a frictionless in-repo debug environment with VS Code launch profiles, unified debug runner script with mock and live .env support, and pytest pdb integration
